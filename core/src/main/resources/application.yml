spring:
  datasource:
    driverClassName: org.mariadb.jdbc.Driver
    url: jdbc:mysql://localhost:3306/product_reviews
    username: your_username
    password: your_password
  jpa:
    hibernate.ddl-auto: create
    generate-ddl: true
    show-sql: true
  messages:
    encoding: UTF-8

server:
  port: 9098